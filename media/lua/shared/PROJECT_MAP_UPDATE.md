# PROJECT_MAP_UPDATE.md

## 2026-06-08 — road coordinate guard + ProjectZomboid class cleanup

### Fixed

- Stopped repeated server-side `__add not defined for operands in FindNearbyWorldRoadPoint` errors when checkpoint generation receives invalid or non-numeric virtual group coordinates.
- Added defensive numeric validation in road navigation and checkpoint selection path:
  - `NPCRoadNavBridge.FindNearbyWorldRoadPoint`
  - `NPCRoadNavBridge.FindNearbyWorldRoadStepToward`
  - `NPCRoadNavBridge.FindWorldRoadPoint`
  - `NPCRoadNavBridge.FindLoadedPreferredAround`
  - `NPCWorldDirectorBridge.GetNearbyRoadPoint`
  - `NPCCheckpointsServerBridge` strategic checkpoint selection
- Cleaned the full fixed mod package from accidental broad `media/ProjectZomboid/zombie/*.class` overrides. The client log showed `IsoTree$TreeShader.initShader` failing with `NoSuchMethodError`, which was caused by stale engine classes being loaded from the mod.

### Compatibility notes

- Existing checkpoint/road-patrol behavior is preserved for valid coordinates.
- Invalid coordinates are skipped safely instead of breaking the server update event.
- Existing ModData, network commands, faction data, checkpoint data and save format are unchanged.

## 2026-06-08 — Stage 364 render rollback after OpenGL 1282 reports

### Observed chain
- Client log still reported repeated `OpenGLException: Invalid operation (1282)` during character rendering.
- The active stack was `Shader.setLight` / `Shader.startCharacter` / `Model.DrawChar` through `SpriteRenderer.postRender` and `SpriteRenderer.buildStateDrawBuffer`.
- This was not the old `IsoTree$TreeShader.initShader` `NoSuchMethodError` chain and not the server road checkpoint `__add` chain.

### Minimal fix
- Rolled back the aggressive 2026-06-07 render buffer override set to the known-good 2026-06-05/STABLE render class set.
- Removed the extra `SpriteRenderer$1.class` generated by the newer render patch.
- Restored `ProjectZomboid64.json` to the stable VM args without `br.render.*` overrides.
- Kept the Lua road-coordinate guards from Stage 363.
- Kept pathfinding/PolygonalMap class set unchanged.

### Changed files
- `media/ProjectZomboid/ProjectZomboid64.json`
- `media/ProjectZomboid/zombie/core/SpriteRenderer.class`
- `media/ProjectZomboid/zombie/core/SpriteRenderer$RingBuffer.class`
- `media/ProjectZomboid/zombie/core/SpriteRenderer$RingBuffer$StateRun.class`
- `media/ProjectZomboid/zombie/core/SpriteRenderer$WallShaderTexRender.class`
- `media/ProjectZomboid/zombie/core/SpriteRenderer$s_performance.class`
- `media/ProjectZomboid/zombie/core/sprite/GenericSpriteRenderState.class`
- removed: `media/ProjectZomboid/zombie/core/SpriteRenderer$1.class`
- `DELETE_EXTRA_PROJECTZOMBOID_FILES.txt`

### Compatibility note
This rollback only touches the render override layer. Lua gameplay, world director data, ModData contracts, network commands, checkpoint data, base data and NPC behavior APIs are unchanged.

## 2026-06-08 Stage365 - vanilla graphics restore

- Restored the Project Zomboid character/sprite renderer to vanilla by removing mod-provided `zombie/core/SpriteRenderer*` classes.
- Removed mod-provided `zombie/core/sprite/*` renderer state classes.
- Kept the Stage363/Stage364 Lua road/checkpoint coordinate guards.
- Kept non-render pathfinding Java overrides under `zombie/vehicles/` unchanged.
- Added `DELETE_GRAPHICS_ENGINE_PATCH_STAGE365.txt` for manual installs that overlay patches instead of replacing the full mod folder.


## Stage366 — full vanilla ProjectZomboid runtime / no Java class overrides (2026-06-08)

Reason: fresh client logs still showed `NoSuchMethodError` in `IsoTree$TreeShader.initShader` and repeated OpenGL/SmartTexture failures. Stage365 archive itself did not contain `IsoTree.class`, so the observed stack means stale `media/ProjectZomboid` files remained in the installed mod/workshop copy or cache.

Change:
- Removed the entire `media/ProjectZomboid` override folder from the distributed mod archive.
- This restores vanilla graphics/runtime classes completely.
- This also temporarily disables the Java pathfinding/render engine patch layer; Lua gameplay fixes from Stage363/364/365 remain.
- Added `DELETE_EXTRA_PROJECTZOMBOID_FILES.txt` and a local cleanup BAT for manual installations.

Install requirement:
- Do not overlay this build on top of an old folder. Delete the installed `FactionsConfrontation` folder first, then extract/copy Stage366.
- For Workshop tests, force a clean local cache/resubscribe if stale files remain.


## 2026-06-08 Stage367 — Black Market Defend Zone completion recovery

- Fixed a Defend Zone edge case where the third wave could show `3/3, left 0` while the Black Market still treated the defense contract as active.
- If the final wave is cleared while the Black Market reward square is not loaded, the quest now moves to `reward_pending` instead of staying stuck.
- Added retry/claim path: returning to the Black Market and using the contract menu can now place the reward and complete the pending defense contract.
- Removed the empty defense-zone map marker once the zone is cleared and the reward is pending.
- Changed only the Black Market defense quest server/client bridge files; no Java, render, pathfinding, save-format, or unrelated NPC behavior changes.

## Stage 368 - Hired mercenary order/formation stability
- Stabilized hired blue mercenary command execution without restoring any Java/class overrides.
- Formation slot selection now uses positive stable slot IDs from memberIndex/slotIndex/formationIndex or parsed string IDs such as `bandit-70`, instead of collapsing several mercenaries into slot 1.
- Server mercenary order payloads now carry member/slot indexes to the client so Follow/Hold/Guard/Tactical commands keep squad spacing after network sync.
- Manual order batches now preserve urgent clear-combat delivery instead of forcing every squad member through the delayed async queue.
- Group hire/order application preserves missing memberIndex on stored group members, improving control after hire, save/load, and repeated commands.
- Also hardened client strict-follow fallback, companion/tactical index helpers, and manual loot positioning against non-numeric NPC ids.
- Scope: Lua-only command/formation synchronization; no `media/ProjectZomboid`, no `.class` files, no save-format/API rename.


## Stage 369 - Direct hard Follow for hired blue mercenaries
- Changed the player `Follow` order for hired blue mercenaries from formation/slot following to direct player-position chasing.
- Follow now targets the owner's current `x/y/z` position, refreshes that target while the player moves, and uses Run with a very small arrival radius instead of holding a formation offset.
- Disabled follow-slot/leader-offset behavior for strict hired Follow; Hold and Guard still use formation/anchor slots.
- Added direct-follow flags (`directPlayerFollow`, `noFormation`) and reduced the strict Follow leash so watchdog/reapply logic corrects drift much earlier.
- Scope: Lua-only companion/order execution; no `media/ProjectZomboid`, no `.class` files, no save-format/API rename.


## Stage 370 - Bodyguard Follow and order matrix hardening
- Supersedes Stage369 direct-to-player Follow for hired blue mercenaries. Follow is again a formation order, but with tight bodyguard slots around the player instead of wide trailing offsets or a single direct target tile.
- `NPCFormationSlotsBridge.GetPlayerFollowSlotPoint` now provides close shoulder/bodyguard/line/wedge/wide slots around the player's live facing direction. Formation changes while Follow is active retarget moving guards without stopping the order.
- `NPCProgramCompanionBridge`, `NPCBrainDirectorBridge`, and `NPCUpdateBridge` now use the same bodyguard Follow spacing, quick target refresh, and non-stationary movement task flags. Direct `directPlayerFollow` / `noFormation` Follow flags were removed from the active order path.
- Patrol and house-search/manual-loot orders no longer queue Shrug/LootLow/Shove/window-smash style ambient hand animations for hired manual orders; they use safe Idle/FaceLocation filler and keep movement tasks active so weapon/hand gestures do not trap the NPC.
- Patrol is now a rotating waypoint order around the player-selected anchor instead of standing at one ring slot after arrival.
- Fire-mode orders were separated from hard movement interruptions: Defensive/ReturnFire/DangerClose/Suppress/FireAtWill apply as tactical overlays, while HoldFire and MeleeOnly still clear active combat.
- Server mercenary order payloads now treat any player-commanded mercenary order as urgent for client sync, reducing the perceived one-NPC-at-a-time delayed application.
- Scope: Lua-only command, follow, patrol/search, sync, and fire-mode behavior. No Java/class override layer, no `media/ProjectZomboid`, no save-format/API rename.


## Stage 371 - Hired mercenary order system refactor / responsiveness

- Refactored only the hired blue mercenary order dispatch/execution path; no Java/class override layer, no `media/ProjectZomboid`, no quests, economy, bases, loot tables, or save-format/API rename.
- Server mercenary group orders now create one shared `orderRevision`/`orderBatchId` and propagate it to the group and every affected mercenary brain/order.
- Server sync no longer splits player mercenary orders into delayed per-NPC async updates. It sends one urgent `MercenaryOrderBatch` marked `applyTogether`, `forceImmediateOrder`, and non-async for all active members.
- Client order handling now applies the entire batch in two phases: first all brains receive the same order state, then all loaded mercenaries immediately queue their generated order task. This removes the perceived one-NPC-at-a-time command execution.
- Manual order consumption now keys primarily on `orderRevision`, not only issued time/sequence, so repeated quick orders and formation/fire changes cannot be ignored as stale duplicates.
- Immediate Follow task generation was tightened for responsiveness: smaller arrival radius, lower path throttle, shorter movement intent TTL, and immediate reapply when player/bodyguard slots move.
- Patrol/SearchHouse/LootArea manual orders avoid hand/face ambient loops after reaching a scan point; they use short idle scan ticks and re-enter movement selection quickly, preventing weapon-hand animation stalls.
- Scope: Lua-side mercenary command dispatch, client sync, immediate task enqueue, and order-specific patrol/search/follow execution only.

## Stage 372 - Dedicated hired mercenary direct order pipeline

- Hired blue mercenary orders are now separated from the shared NPC order contract dispatch path.
- `NPCMercenaryContract` now builds player mercenary orders directly with `source=mercenary_direct`, `playerCommandMode=mercenary_direct`, `directMercenaryOrder=true`, own sequence, strict/sticky/follow/fire/formation fields, and without calling `NPCOrderContract.Set` for hire or player orders.
- Server order sync now sends `MercenaryDirectOrderBatch` instead of the generic mercenary update batch. The payload remains server-authoritative but is explicitly marked as direct mercenary order data.
- Client command handling now has a dedicated `MercenaryDirectOrderBatch` path: it applies all order states first, then queues immediate tasks for every loaded hired mercenary in the same batch, without routing through the generic `UpdateNPCPart`/async order queue.
- Runtime order maintenance in `NPCUpdateBridge` recognizes direct mercenary orders independently, so strict/follow/tactical/manual order reapply no longer depends on shared order-contract active-state checks.
- Existing old `MercenaryOrderBatch` remains as compatibility fallback, but direct hired orders use the dedicated path.
- Scope: hired mercenary order/dispatch/runtime only. No Java/class override layer, no `media/ProjectZomboid`, no quests, economy, bases, loot tables, or save-format/API rename.


## Stage 373 - Mercenary direct order hotfix
- Fixed a Stage372 client-side Lua error in `NPCUpdateBridge.lua`: strict mercenary order code called `bridge_normMercenaryOrderName` before its local function was in scope.
- The error happened during order execution/watchdog checks after server accepted the order, so mercenary commands could look unresponsive even though the server logged `order result ... changed=4 members=3`.
- Kept Stage372 direct mercenary pipeline intact; this patch only repairs the helper binding so Follow/Guard/Patrol/fire-mode watchdog logic can run instead of throwing `Object tried to call nil in GetStrictOrderBoundaryAnchor`.
- Scope: client Lua hotfix for mercenary order execution only; no Java, no graphics, no world director, no bases, no economy, no save/API rename.

## Stage375_MERCENARY_CURSOR_SEMANTICS_ROLLBACK (2026-06-09)
- Emergency stabilization after Stage374: removed the new direct runtime-controller from the release line by rebasing on Stage373 hotfix.
- Restored the stable Stage373 mercenary direct pipeline and added conservative cursor semantics.
- Follow remains player/bodyguard-follow and does not use the context-menu cursor square.
- Point orders with cursor anchors now move to the exact clicked world point first: Hold, Guard, Return, Advance, FallBack, Flank, Encircle, BackToBack, TakeCover, WatchSector.
- Strict-order boundary checks now measure Guard/Return/FallBack and other anchored point orders against the order anchor, not against the player.
- No Java, no media/ProjectZomboid, no base/global-war/quest/economy changes.

## Stage376 - Mercenary point-order semantics hardening
- Scope limited to hired blue mercenary order system only.
- Client menu now tags mercenary commands as `targetType=player`, `targetType=point`, or `targetType=overlay`.
- `Follow` no longer sends or preserves a cursor/world anchor; it is explicitly player-targeted.
- Point orders now send the right-clicked world square as the only authoritative cursor anchor: Hold, Guard, Patrol, Loot/LootHouse/body-loot, RearmHere, Return, and tactical point commands.
- Server validates point orders before applying them and rejects point orders without a usable world anchor instead of falling back to player/NPC position silently.
- `NPCMercenaryContract` preserves point anchors only for point orders, clears stale anchors on Follow, and marks orders with `targetType`/`anchorMode` for downstream execution.
- Client execution keeps exact cursor movement for final-position orders, while patrol/loot/search orders keep their cursor anchor for the existing behavior layer instead of being converted to player-relative orders.
- No global-war, base, Black Market, economy, quest, spawn, persistence format, Java, or graphics systems were changed.

## Stage377 - Mercenary ownership validation hardening
- Scope limited to hired blue mercenary order security/ownership checks.
- Server now rejects `MercenaryGroupOrder` when the client supplies a `groupId` that is not hired by the requesting player.
- Removed the unsafe redirect behavior where a stale or non-owned requested `groupId` could fall back to the player's active hired squad and make it look like any blue NPC accepted commands.
- Physical mercenary order fallback to "any hired group" is only allowed when the command did not explicitly target a group.
- Client context menu no longer replaces a directly clicked NPC with a nearby hired mercenary. Nearby hired-merc fallback is used only when no NPC was directly clicked, preventing non-hired blue NPCs from showing/behaving like the player's squad.
- No global-war, base, Black Market, economy, quest, spawn, persistence format, Java, or graphics systems were changed.

## Stage378 - Mercenary hire server-side validation hardening
- Scope limited to blue mercenary hire/order validation only.
- `HireMercenaryGroup` no longer trusts client-provided `args.mercenary` or `args.brainSide` to decide whether a target is hireable.
- Server now requires the resolved server-side brain/group to be blue/mercenary/hireable, or already hired by the requesting player.
- Server now checks that the hire target is actually near the player using server-side NPC/brain/group coordinates, not client-supplied intent alone.
- Added a safe physical-NPC corroboration pass for hire targets that were first resolved from queued brain data, reducing false rejects when queue coordinates are stale.
- Payment behavior was intentionally not changed in this stage to avoid breaking COOP/MP inventory handling.
- No global-war, base, Black Market, prisoner, disguise, debug/admin, economy, quest, spawn, persistence format, Java, or graphics systems were changed.

## Stage379 - Mercenary order input guard hardening
- Scope limited to hired blue mercenary command input validation only.
- `MercenaryGroupOrder` now validates a cached/active group id before applying an order; stale active group ids are ignored instead of being treated as authoritative targets.
- Empty/malformed mercenary command packets without an order name or overlay (`fireMode`, `formation`, `followDistance`) are rejected before any order revision is created.
- Point-order cursor-anchor validation now happens before throttle accounting, so bad/no-anchor packets cannot consume the short order throttle window and block the next valid player click.
- Existing player-owned fallback behavior is preserved for commands without an explicit group id; explicit non-owned group ids still reject immediately.
- No AI behavior, global-war, base, Black Market, prisoner, disguise, debug/admin, economy, quest, spawn, persistence format, Java, or graphics systems were changed.

## Stage380 - Mercenary client-side command preflight hardening
- Scope limited to hired blue mercenary order/menu validation only.
- Client context-menu order dispatch now refuses explicit `groupId` commands when the local mercenary state does not show that squad as hired by the player.
- `AddMercenaryOrdersMenu` no longer renders a squad-specific order submenu for a group that is not locally marked as the player's hired squad.
- Point orders no longer build a `player_fallback` anchor if no world square is available; the client now stops before sending and asks the player to right-click a target point.
- Point orders now explicitly mark `cursorAnchor=true` in the client payload to match the server-side cursor-anchor contract.
- `MercenaryOrderStatus` now rejects explicit non-owned group ids instead of falling back to another hired squad's status.
- No AI behavior, movement execution, hire/payment, global-war, base, Black Market, prisoner, disguise, debug/admin, economy, quest, spawn, persistence format, Java, or graphics systems were changed.


## Stage381 — Mercenary hire other-owner guard + media project map

Scope: mercenary command/hire safety only; documentation location update.

Changes:
- Added a server-side guard in `NPCClientCommandsServerBridge.lua` so `HireMercenaryGroup` rejects a target brain that is already hired by another player even if the virtual group lookup is missing/stale.
- Added root-level project map copies into `FactionsConfrontation/media/PROJECT_MAP.md` and `FactionsConfrontation/media/PROJECT_MAP_UPDATE.md` so the navigation/changelog files live inside the loaded mod media tree.

Not changed: AI movement, order execution, Black Market, bases, payment/inventory, prisoner/disguise, global war, persistence format, Java/classes, graphics.

## Stage382 — Mercenary point-anchor distance guard

Scope: hired mercenary order validation only.

Changes:
- Added a server-side distance/z-level guard for point-based mercenary orders before throttle/orderRevision.
- Malicious or stale point-order payloads with world anchors far away from the player are now rejected without creating a new mercenary order revision.
- `MercenaryOrderStatus` only falls back to another owned squad when the player did not explicitly request a group id; explicit group status no longer silently switches to a different squad when empty/stale.
- No AI movement, order execution, hire/payment, Black Market, bases, prisoner/disguise, global war, persistence format, Java, graphics, or `media/ProjectZomboid` changes.


## Stage383 — Mercenary order telemetry

Scope: hired mercenary order diagnostics only.

Changes:
- Added client-side trace fields to mercenary hire/status/order commands: `clientTraceId`, `clientSendMs`, `clientOrderSeq`, `clientEvent`.
- Added server-side telemetry logs for mercenary order request/reject/throttle/result and hire request using the same trace id.
- Added telemetry propagation into `MercenaryDirectOrderBatch` so the client can log batch receive timing and immediate-task queue timing.
- This patch does not change AI execution, movement, point-order semantics, hire validation, payment/inventory, Black Market, bases, global war, persistence format, Java/classes, graphics, or `media/ProjectZomboid`.

Useful log markers:
- `[NPCMercenary] order click trace=...`
- `[NPCMercenary] order request ... trace=... clientToServerMs=...`
- `[NPCMercenary] order result ... revision=... trace=...`
- `[NPCMercenaryTelemetry] client batch received ... clientTotalMs=...`
- `[NPCMercenaryTelemetry] client immediate task queued ...`
## Stage384 — Mercenary Menu Consistency

Scope: client-side mercenary context menu consistency only.

Changes:
- Suppressed the generic hired-mercenary order root when the player right-clicks an unowned blue mercenary NPC, so global bodyguard orders are not visually confused with orders for that clicked NPC.
- Squad-specific orders on a clicked blue mercenary are shown only when the clicked NPC has a valid groupId that is locally known as hired by this player.
- Hire option is hidden for blue mercenaries that are already marked as hired by another player or whose group is marked as hired by another player.
- No AI, movement, server execution, payment, Black Market, bases, persistence, Java or ProjectZomboid class files were changed.

## Stage385 — Mercenary final safety pass

Scope: hired mercenary command validation only.

Changes:
- Added server-side normalization/allow-listing for mercenary `fireMode` values: `FireAtWill`, `Defensive`, `HoldFire`, `MeleeOnly`, `ReturnFire`, `DangerClose`, `Suppress`.
- Added server-side normalization/allow-listing for mercenary `formation` values: `close`, `ring/bodyguard`, `wide`, `line`, `wedge`.
- Clamped `followDistance` to a safe `0.75..8.0` range before writing order data, preventing malformed payloads from producing huge formation offsets.
- Invalid overlay payloads are rejected before throttle/orderRevision creation.
- No AI, movement, hire/payment, Black Market, bases, global war, persistence, Java/classes, graphics, or `media/ProjectZomboid` changes.

## Stage386 — Mercenary unified immediate execution

Scope: client-side hired mercenary order execution only.

Changes:
- Added a mercenary order movement-runtime reset that clears stale path2/pathfind behavior, movement throttles, old follow-slot runtime and stale moving state only when a new direct mercenary order token is received or a strict order is explicitly re-applied.
- `Follow` now uses the same immediate movement lane as working point orders: live player bodyguard slot -> preemptive Move task -> short path throttle.
- Follow task compliance now detects stale follow-slot targets; when the player moves and the current Move task points to an old slot, the strict watchdog can re-queue a fresh Follow movement task instead of treating the old manual task as compliant forever.
- `Patrol`, `Loot`, `LootHouse`, `LootBodies*`, and `RearmHere` now first queue a direct Move task to the cursor anchor; legacy patrol/search/loot/rearm behavior is allowed only after the mercenary reaches the anchor.
- Direct point/follow movement tasks are marked as player/manual/preemptive order tasks with unified immediate flags.
- No server validation, hire/payment, Black Market, bases, global war, persistence, Java/classes, graphics, or `media/ProjectZomboid` changes.

## Stage387 — Mercenary direct-order execution lane

Scope: client-side hired mercenary order execution only.

Changes:
- Direct mercenary player orders no longer force-switch the NPC back into `Companion` / `CompanionGuard` program during combat/movement cleanup. This prevents the old companion program from competing with accepted direct orders.
- Added a generalized player-mercenary order maintenance lane before strict watchdog/combat generation. Follow/Hold/Guard/Patrol/Return and tactical point orders are preserved above ambient combat/program logic instead of relying only on the tactical-command path.
- Upper point commands now use per-member formation slots around the clicked anchor for `Hold`, `Guard`, `Return`, `WatchSector`, and `BackToBack`.
- `Follow` uses wider visible formation slots for non-close formations so line/wedge/ring/wide do not collapse into the compact bodyguard ring.
- `Patrol` now uses a direct lightweight patrol loop around the clicked anchor after arrival, rather than delegating back to legacy companion patrol behavior that could leave the squad frozen.
- No server validation, hire/payment, Black Market, bases, global war, persistence, Java/classes, graphics, or `media/ProjectZomboid` changes.

## Stage388 — Mercenary house search / hold-fire / animation safety

Scope: hired mercenary command polish only, preserving Stage387 direct-order lane.

Changes:
- `LootHouse` / search-house command now requires the clicked square to be inside a building/room on the client side.
- Server rejects `LootHouse` anchors outside buildings before throttle/orderRevision creation.
- `HoldFire` now physically clears primary/secondary hand items and visual hand variables when the order is applied.
- Manual loot/check-corpse/rearm and house-search loot tasks now use the loot/search animation instead of idle while working.
- New direct mercenary order/reset path clears stuck bump/movement animation state before applying the next direct command.
- Did not change Stage387 Follow/Guard/Hold/Patrol direct-order movement lane, hire/payment, Black Market, bases, global war, persistence, Java/classes, graphics, or `media/ProjectZomboid`.

## Stage389 — Mercenary ambient idle animations

Scope: hired mercenary ambient presentation only, preserving the working Stage387/388 direct-order movement lane.

Changes:
- Added a low-priority mercenary ambient idle layer that runs only when a hired mercenary has no current task, is not moving/pathfinding, has no combat target/threat, and has been settled long enough after the last player order.
- Settled Follow/Hold/Guard states can now occasionally play safe idle bump animations such as Smoke, Shrug, ShiftWeight, SitAction, SitRubHands, ChewNails, WipeBrow, and PullAtCollar.
- Search/loot/rearm states can now occasionally play search-style ambient animations such as LootLow, Loot, Forage, and SitAction after reaching the cursor/building anchor when no immediate loot task is available.
- Time-action timeout support now also covers mercenary ambient idle tasks so non-looped bump animations cannot hold the NPC indefinitely.
- The action router recognizes the new ambient/search bump animations as low-priority animation-lane tasks and still blocks them while the engine is moving/pathfinding.
- No changes to server validation, hire/payment, Black Market, bases, global war, persistence, Java/classes, graphics, `media/ProjectZomboid`, or the Stage387 direct movement/order lane.


## Stage390 — Mercenary search-house animation and fire recovery

Scope: hired mercenary animation/combat polish only, preserving the working Stage387 direct movement/order lane.

Changes:
- `LootHouse` no longer delegates to the old companion house-search program after reaching the clicked building anchor; it uses a direct in-building search lane with valid building/room points and vanilla search/inspection animations.
- New direct-order animation reset clears crouch/sneak/aim/moving/pathfind variables so SearchHouse/Forage/LootLow poses do not carry over into Follow.
- Player-command Time tasks also clear crouch/sneak/pathfind variables when complete.
- `HoldFire` remembers the previously held weapon type before clearing hands; combat fire modes can re-equip a stored/inventory firearm on the client.
- Direct mercenary orders now permit nearby combat preemption for valid fire modes, allowing FireAtWill/Defensive/etc. to shoot zombies/enemies while preserving movement when no threat exists.
- No changes to hire/payment, Black Market, bases, global war, persistence, Java/classes, graphics, `media/ProjectZomboid`, or the Stage387 movement lane.

## Stage391 — Mercenary hold-position combat overlay

Scope: hired mercenary combat-overlay behavior only, preserving the working Stage387/390 direct movement/order lane.

Changes:
- Direct player-commanded mercenaries can still aim, shoot, reload, equip, shove, or hit during FireAtWill/Defensive/ReturnFire/DangerClose/Suppress combat preemption.
- Combat-generated chase/path tasks are filtered out for direct mercenary orders, so NPCs should not abandon Follow/Guard/Hold/Patrol/SearchHouse positions to pursue zombies or enemies.
- If combat AI wants to chase but movement is blocked, a short FaceLocation hold-position task is queued instead so the NPC stops/faces the threat without leaving formation.
- Close melee contact is still allowed only when the target is already within immediate reach; distant melee approach movement is denied.
- No changes to hire/payment, Black Market, bases, global war, persistence, Java/classes, graphics, `media/ProjectZomboid`, or the Stage387 movement lane.


## Stage392 — Mercenary friendly-fire immunity and priority retaliation

Scope: hired mercenary combat safety only, preserving the working Stage387/391 direct movement/order lane.

Changes:
- Hired mercenaries now ignore friendly hits from their hiring player and same-owner hired mercenaries. The client restores any immediate vanilla health loss and does not trigger faction punishment/hostility for owner/squad friendly fire.
- NPC firearm and melee damage filters now treat same-owner hired mercenaries and their hiring player as friendly targets, so mercenary bullets/melee cannot damage their owner or other mercenaries owned by the same player.
- When a hired mercenary is damaged by a hostile actor, the attacker is stored as a same-owner mercenary retaliation target. All nearby same-owner hired mercenaries receive that target as a high-priority combat target.
- Retaliation temporarily overrides HoldFire/MeleeOnly equipment gating only for the attacker, allowing mercenaries to re-equip and return fire. This does not permanently change the player's selected fire mode/order state.
- Retaliation still uses the Stage391 hold-position combat overlay: mercenaries may aim/shoot/reload/equip and face the attacker, but chase/Move/GoTo tasks remain blocked.
- When the retaliation target dies or disappears/ages out, the squad naturally returns to normal zombie/enemy scanning and the current direct order.
- No changes to hire/payment, Black Market, bases, global war, persistence, Java/classes, graphics, `media/ProjectZomboid`, or the Stage387 movement lane.


## Stage393 — Mercenary owner zombie threat shooting

Scope: hired mercenary combat targeting only, preserving the working Stage387/391 direct movement/order lane.

Changes:
- Direct player-commanded mercenaries in FireAtWill/Defensive/ReturnFire/DangerClose/Suppress bypass the generic combat scan scheduler gate so nearby threats can be evaluated while Follow/Guard/Hold is active.
- Added an owner-protection zombie scan: zombies that target the hiring player, or are already inside the owner's close danger bubble, become priority zombie targets for the hired squad.
- The owner-threat lane only generates stationary combat behavior: aim, shoot, reload, equip, and face-target. It does not add chase movement.
- Strict player-order compliance now explicitly permits ranged hold-position combat tasks, preventing Follow from immediately overwriting a valid Aim/Shoot task against a zombie near the owner.
- No changes to movement/pathing, hire/payment, Black Market, bases, global war, persistence, Java/classes, graphics, `media/ProjectZomboid`, or the Stage387 movement lane.

## Stage447 — Door open fallback + NPC vanilla-zombie brain guard

Date: 2026-06-10

Changed files:
- `media/lua/shared/NPCActions/NPCActionDestroyBridge.lua`
- `media/lua/server/NPCServer/NPCClientCommandsServerBridge.lua`
- `media/lua/client/NPCClient/NPCUpdateBridge.lua`
- `media/PROJECT_MAP.md`
- `media/PROJECT_MAP_UPDATE.md`

Purpose:
- make living/materialized NPCs open normal doors before using the old breach/destroy task;
- keep breach damage only for barricaded/blocked doors and generic thumpable objects;
- prevent a marked live NPC from briefly falling back into vanilla zombie target/lunge/attack behavior while still showing an NPC marker.

Implementation notes:
- `DestroyObject` now routes usable door targets through `OpenDoorObject` first on the server;
- client-side Destroy action also tries `OpenDoor` first for normal door objects;
- live NPC update now clears vanilla zombie target/aggro/lunge state through `EnforceLiveNPCNotVanillaZombie`;
- the guard is applied on mark-as-NPC, before zombie/NPC update separation, when physical LOD skips a frame, and when the NPC is outside the light cache.

Safety notes:
- no Java/class changes;
- no save/network/ModData schema changes;
- no faction strategy, mercenary orders, checkpoints, bases, contracts or Black Market logic changed;
- NPC combat tasks still use the mod's own task/brain/faction checks; only vanilla zombie brain targeting is suppressed for live NPC actors.
